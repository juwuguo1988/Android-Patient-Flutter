import 'package:flutter/material.dart';

class UricHistory extends StatelessWidget {
  UricHistory();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('尿酸历史'),
      ),
      body: Container(
          color: Colors.white,
          child: Text("data")),
    );
  }
}
